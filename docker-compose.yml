version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: cadam-frontend:latest
    container_name: cadam-frontend
    ports:
      - '3000:80'
    restart: unless-stopped
